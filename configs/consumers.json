{
  "version": "v1.0-dev",
  "consumers": [
    {
      "consumer_id": "dev_consumer",
      "api_key_id": "dev-key-default",
      "api_key": "dev-key",
      "permissions": [
        "kb:project_docs:read",
        "tool:doc_search:use",
        "tool:evidence_check:use",
        "tool:image_generation:use",
        "tool:video_generation:use",
        "tool:inspection:use",
        "model:coding:use",
        "model:writing:use"
      ],
      "allowed_workflows": ["*"],
      "allowed_tools": ["*"],
      "rate_limit_key": "consumer:dev_consumer"
    }
  ]
}
